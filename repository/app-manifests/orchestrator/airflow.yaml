apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: airflow
  namespace: cicd
spec:
  project: default
  source:
    repoURL: "https://github.com/vsvale/kappa_k8s_config.git"
    path: repository/yamls/orchestrator/airflow/
    targetRevision: HEAD
    helm:
      valueFiles:
        - values-development.yaml
  destination:
    server: "https://kubernetes.default.svc"
    namespace: orchestrator
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
