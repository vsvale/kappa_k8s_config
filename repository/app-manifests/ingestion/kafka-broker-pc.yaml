apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kafka-broker-pc
  namespace: cicd
  annotations:
    argocd.argoproj.io/hook: Skip
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default
  source:
    repoURL: "https://github.com/vsvale/kappa_k8s_config.git"
    path: repository/yamls/ingestion/broker_persistent_claim/
    targetRevision: HEAD
    directory:
      recurse: true
      jsonnet: {}
  destination:
    server: "https://kubernetes.default.svc"
    namespace: ingestion
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
